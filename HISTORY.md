# Claude Session Manager 開発履歴

本ドキュメントはCSM（Claude Session Manager）の開発経緯を時系列で記録したものである。
各バージョンで何をしたか、なぜそうしたか、品質管理部との連携で何が発見されたかを含む。

---

## v0.1.0 (2026-03-28) — 初回リリース

### 経緯
Claude Codeのセッション管理をVS Codeサイドバーから行いたいという要件で開発開始。
`~/.claude/projects/` 配下のJSONLファイルを解析してセッション一覧を表示する基本機能を実装。

### 主な機能
- 会話一覧（日付別グループ、ブックマーク、タグ、メモ、リネーム、検索）
- ライブセッション検出（PID生存チェック）
- メモリ管理（タイプ別バッジ、プログレスバー、統合・抽出）

### 技術判断
- **データソース**: JONLファイルを直接パースする方式を選択。Claude Code CLIにAPI的なインターフェースがないため
- **ライブ検出**: `sessions/*.json` のPIDを `process.kill(pid, 0)` で確認する方式。Windowsでも動作する
- **メモリ管理**: `~/.claude/projects/<encoded>/memory/` を対象に、frontmatter解析でtype分類

---

## v0.1.1〜v0.1.4 (2026-03-28〜03-30) — Marketplace公開・微調整

### 経緯
v0.1.0完成後すぐにVS Code Marketplaceへ初回公開。Publisher ID `ratorin` で登録。
公開後に表示名やアイコンの調整、メモリ管理のOneDrive対応などを実施。

### 主な変更
- v0.1.1: displayNameにサブタイトル追加、Marketplace初回公開
- v0.1.2: メモリファイルに行数表示
- v0.1.3: 複数PC環境での変更統合、MEMORY.md表示/編集対応
- v0.1.4: OneDrive配下のメモリフォルダ対応

### 技術判断
- **OneDrive対応**: ユーザーのホームがOneDrive配下にある場合、パス解決が変わる問題。パス正規化で対応

---

## v0.1.7〜v0.1.9 (2026-03-31〜04-01) — エージェント管理の原型

### 経緯
マルチエージェント運用（取締役+子エージェント構成）の需要が高まり、エージェント管理機能の開発に着手。
この段階では `agents.md` をパースする方式で実装していた。

### 主な変更
- エージェント登録・役割タグ・ルールファイル編集
- `subagents/` フォルダ内の子エージェントをツリー表示
- Markdownレンダリング、リンクのクリック対応
- 組織図（取締役トップノード + 階層カード表示）
- ステータスバーにエージェント情報表示

### 技術判断
- **MD→JSON移行の伏線**: agents.mdのパースは文字列操作が脆弱で、特殊文字や改行で壊れやすかった。v0.2.0でJSON化する判断の根拠になった

---

## v0.2.0 (2026-04-02) — エージェント管理基盤の再設計

### 経緯
agents.md方式の限界（パース脆弱性、データ整合性、部分更新の困難さ）から、`session-manager.json` への完全移行を決断。
これに合わせてUIも全面改訂。

### 主な変更
- **データソース統一**: agents.md廃止 → `session-manager.json` の `agents[]` 配列に一本化
- **Webviewフォーム**: QuickPickステップ式 → 1画面Webviewパネルに置換
- **エージェントプレビュー/設定分離**: 読み取り専用プレビュー + 編集フォーム
- **組織図**: 取締役トップ + ⚡(Claude Code起動) / 🕐(履歴表示) ボタン
- **コンテキストメニュー整理**: contextValue 4種による出し分け
- セッション削除（`.trash/`移動、rm禁止ルール準拠）
- ソート7種、グループ化4種（日付別/タグ別/エージェント別/フラット）
- ウェルカム画面（取締役プリセットで即座に開始）
- ルールフォルダ設定（`defaultRuleFolder`）

### 技術判断
- **JSON vs SQLite**: session-manager.jsonを選択。理由は①ファイル1つで完結、②他エージェントからも読み書き可能、③VS Code拡張にSQLiteバインディングは重すぎる
- **Webviewフォーム**: QuickPickの連続ダイアログはUXが悪く、項目が増えると破綻する。Webview化で全項目を一覧可能に
- **セッション削除はゴミ箱方式**: ユーザーのフィードバック「rm禁止、.trash/にmvで移動する」を遵守

---

## v0.2.1 (2026-04-02) — アイコン改善・ステータスバー

### 主な変更
- 組織図のアイコンをオレンジ角丸背景+白抜きスパークデザインに変更
- ソート基準5種→7種に拡張（「日付」を「作成日」と「更新日」に分割）
- ステータスバーで動作中エージェント名を表示（ファイル監視方式）

---

## v0.2.2 (2026-04-02) — バグ修正3件

### 発見経緯
実運用で3件のバグが顕在化。

1. **ポーリング間隔が設定変更に追随しない**: `pollTimer` が起動時値で固定。`restartPolling()` メソッドを追加
2. **プレビューパネルのリスナー累積**: `onDidReceiveMessage` が再バインド時に累積。Disposable管理で修正
3. **ウェルカム画面が常時表示**: `when: "true"` → `when: "!claudeManager.hasAgents"` に修正

---

## v0.2.3〜v0.2.4 (2026-04-02) — エージェント検出方式の試行錯誤

### 経緯
エージェントの稼働状態をリアルタイム検知する方式を模索した時期。

### v0.2.3: PIDマッピングファイル方式
- `/c/tmp/agent_*.txt` の更新時刻監視 → `.agent-rules/tmp/.agent_pid_{PID}_{名前}` に変更
- 取締役がファイルを作成、完了時に削除。CSMが監視
- **問題**: 取締役側の管理負担が大きく、ファイル作成忘れで検出漏れが頻発

### v0.2.4: 組織図軽量化
- 子エージェントのライブ状態インジケーターを削除（パフォーマンス理由）
- CSS最小化、不要アニメーション削除、イベント委譲に変更

### 技術判断（後のv0.2.6〜v0.3.0で覆される）
- PIDマッピングファイル方式はホスト側（取締役）の協力が前提。自律的な検出には向かなかった
- この経験がv0.2.6のJSONL mtime方式、v0.3.0のPID直接検出方式への移行を後押しした

---

## v0.2.5 (2026-04-02) — ポーリング制御・テンプレート強化

### 主な変更
- `enablePolling` 設定追加（デフォルト: false）— PC負荷を気にするユーザー向け
- ルールファイルテンプレートにMEMORY.md確認・報告先指示を自動追加
- 「セッションを新しくする」で自動遺言生成（直近やり取りからサマリー）
- エージェント起動時にruleFile未設定/不存在の警告表示

### 技術判断
- **自動遺言**: JSONL末尾50行から最後の2往復を抽出。手動入力よりも引き継ぎの質が向上した
- **ポーリングデフォルトOFF**: 常時ポーリングはPC負荷の懸念があり、明示的にONにする設計に

---

## v0.2.6 (2026-04-02) — JSONL解析ベースの監視

### 経緯
PIDマッピングファイル方式の問題を解消するため、セッションJSONLファイルのmtime+末尾解析方式に移行。

### 主な変更
- `agentMonitor.ts` 新設 — tasklist+PIDマッピング方式を完全廃止
- JSONL末尾64KB読み取りでtool_use(Agent/Task)→サブエージェント検出
- mtime+sizeキャッシュで変更時のみ再解析
- 稼働中エージェントが一覧の上に自動ソート

### 技術判断
- **末尾64KB**: JONLファイルは巨大になるため、全体読み込みは非現実的。末尾64KBなら直近のtool_use/tool_resultペアを十分カバーできる
- **mtime判定の限界**: この方式にも「CLIが終了直後はmtimeが新しいまま」という問題が残った。v0.2.7で修正される

---

## v0.2.7 (2026-04-03) — ステータスバー稼働判定のバグ修正

### バグ内容
エージェント終了後も「🟢 3」が減らない。

### 原因分析
mtimeのみの判定では「終了した」vs「しばらく入力がない」を区別できない。Claude Codeの終了処理で最後のデータ書き込みが行われ、mtime閾値内に収まり続ける。

### 修正
- **判定方式をPIDベースに変更**: `isLiveSession(sessionId)` で `sessions/*.json` のPID生存チェックを使用
- CLIプロセス終了時にPIDが消えるため、即座にinactive判定

### 教訓
mtime監視はファイル更新の検知には使えるが、プロセスの生死判定には使えない。PID生存チェックがもっとも信頼性が高い。

---

## v0.2.8 (2026-04-03) — スコープ分離

### 経緯
マルチプロジェクト運用でルールファイルの保存先をプロジェクト単位/グローバル単位で分けたいという要件。

### 主な変更
- AgentConfig に `scope: 'global' | 'project'` フィールド追加
- スコープに基づくルールファイル保存先の自動決定
- フォームをスコープ選択ラジオボタンに変更（手動パス入力廃止）
- MEMORY.md書き込みをメモリファイル+ポインタ方式に改善

### 技術判断
- **手動パス入力の廃止**: ユーザーがフルパスを手入力するのはミスの元。global/projectの2択にすることでルールファイルの置き場を自動管理
- **ポインタ方式**: MEMORY.md自体にコンテンツを書くと肥大化する。ファイル+インデックスポインタ方式に変更

---

## v0.3.0 (2026-04-04) — 監視アーキテクチャ刷新

### 経緯
v0.2.x系で積み上げた監視方式（tasklist→PIDファイル→JSONL mtime→PID直接）の知見を統合し、アーキテクチャを根本から再設計。同時にタスク管理、利用制限モニター、メモリ拡張、フォーム拡張を盛り込んだ大型リリース。

### Phase 1: 監視アーキテクチャ再設計

**判断経緯**:
- v0.2.3のPIDマッピング、v0.2.6のJSONL mtime、v0.2.7のPID直接検出と3回の方式変更を経験
- 問題の本質は「PID生存チェック（セッションの生死）」と「JSONL末尾解析（サブエージェント検出）」が別機能であること
- これらを統合する `AgentWatcher` クラスを新設

**変更内容**:
- `agentMonitor.ts` 削除 → `agentWatcher.ts` + `subagentDetector.ts` に分離
- `setInterval` ポーリング → `fs.watch` + 300msデバウンスに置換
- EventEmitter方式で状態変更を通知（ツリー更新、タスク評価が自動連動）

### Phase 2: ローカル/グローバル分離
- `session-manager.local.json` 導入 — プロジェクト固有エージェントをワークスペース内に保存
- `getAgents()` は同名ローカル優先でマージ

### Phase 3: メモリ管理拡張
- `memoryManager.ts` / `memoryTreeProvider.ts` 新設
- グローバルメモリ (`~/.claude/memory/`) をツリーに表示
- 設定ファイルビューワー（settings.json / settings.local.json / settings.json(project)）

### Phase 4: フォーム拡張
- Effort 4段階（Low/Medium/High/Max）、Max=Opus専用グレーアウト
- Extended Thinking トグル、Haiku時グレーアウト
- Max Thinking Tokens 数値入力、環境変数 `MAX_THINKING_TOKENS` で反映
- モデル変更時のUI連動（onModelChange）

### Phase 5: CLI Builder
- `cliBuilder.ts` 新設 — AgentConfigからCLIコマンドを自動構築

### タスク管理（TaskTracker）
- AgentWatcher イベントでタスク状態を自動評価（running/stalled/completed/error）
- VS Code通知でリアルタイム報告
- stalled閾値60秒、自動クリーンアップ（72h/168h）
- コマンド5件、設定4件追加

### 利用制限モニター（UsageMonitor）
- Anthropic APIの 5h/7d 利用率をステータスバーに表示
- OAuth認証 + `anthropic-beta: oauth-2025-04-20` ヘッダ
- 80%で警告色、95%でエラー色、90%/100%到達で通知
- デフォルトOFF（有効化で軽量APIリクエストが定期発生）

### パフォーマンス改善
- **全readFileSync廃止**: 全ファイルのI/Oを `fs.promises` APIに置換
- dataStore.ts全面async化（TTLキャッシュ維持）
- `Promise.allSettled` による並列解析

### ルールファイル カスタムセクション保持
- `<!-- CSM:AUTO:START -->` / `<!-- CSM:AUTO:END -->` マーカー導入
- 自動生成部分のみ更新し、ユーザーのカスタム記述を保持

### 品質管理部レビューで発見された問題と対応

品質管理部による網羅的コードレビューで以下が検出・修正された。

#### セキュリティ
| ID | 問題 | 対応 |
|----|------|------|
| H-1 | CSM:AUTOマーカーのインジェクション | `sanitizeForAutoSection()` でマーカー文字列を除去 |
| H-5 | WebviewにCSPなし | nonce-based CSP追加（`default-src 'none'`） |
| H-6 | floating promise（未処理reject） | `.catch()` 追加、async/await変換 |

#### 品質
| ID | 問題 | 対応 |
|----|------|------|
| H-2/H-3 | TreeView Disposable未追跡 | createTreeView返り値をsubscriptionsに登録、全EventEmitterにdispose実装 |
| H-4 | dataStore同期I/O | 全面async化（Phase 5で実施） |

#### タスク管理レビュー
| ID | 問題 | 対応 |
|----|------|------|
| H-1 | sessionMtimes不整合 | update()冒頭でclear+再構築 |
| H-2 | outputFileパストラバーサル | `validateOutputFile()` でワークスペース/tmp/.claude配下のみ許可 |
| H-3 | evaluate()で毎回保存 | `batchUpdateTaskLogs()` で1回保存 |
| H-4 | 監視OFF時に状態残留 | running/stalled→pendingリセット + 168h自動クリーンアップ |
| M-1 | stalled閾値未定義 | 60秒に設定（Extended Thinking対応） |

#### 利用率モニターバグ
| 問題 | 原因 | 対応 |
|------|------|------|
| 「取得失敗」常時表示 | HTTPステータスコード未チェック | FetchResult型導入、200以外はエラーメッセージ |
| サブエージェント検出が動かない | hardcoded空配列 | `detectSubagents()` 実呼び出しに修正 |
| 利用率が0.15と表示 | APIは0-1 float返却 | `Math.round(v * 1000) / 10` で0-100%変換 |

---

## 監視方式の変遷まとめ

| バージョン | 方式 | 問題 |
|-----------|------|------|
| v0.2.1 | `/c/tmp/agent_*.txt` mtime | tmpが散らかる、実行中に検出不可 |
| v0.2.3 | PIDマッピングファイル | 取締役側の管理負担、作成忘れで漏れ |
| v0.2.6 | JSONL mtime + 末尾解析 | 終了直後にmtimeが新しいまま残る |
| v0.2.7 | PID生存チェック（直接） | 信頼性高いが単機能 |
| v0.3.0 | AgentWatcher統合（PID + JSONL + fs.watch） | 現行方式。即時検知 |

5回の方式変更を経て「PID生存チェックが最も信頼性が高い」「サブエージェント検出はJSONL解析で別に行う」「ポーリングよりfs.watchが低負荷」という知見に到達した。

---

## 今後の設計検討

- **Stop Hook による即時検知**: 設計案作成済み（`agent_CSM開発部_hook_design.txt`）。フック経由のシグナルファイルでセッション終了を即時通知し、既存のfs.watchをフォールバックとするハイブリッド方式。未実装。
