# 更新履歴

## v0.2.1 (2026-04-02)
### アイコンデザイン改善
- 「Claude Codeで開く」ボタンアイコンをリデザイン
  - オレンジ（#D97706）の角丸四角背景に白抜きスパーク形状を重ねた公式アプリ風デザインに変更
  - `resources/sparkle-dark.svg` / `resources/sparkle-light.svg` を更新
  - 組織図の各エージェントカード・凡例のインラインSVGも同デザインに統一
- ソート機能の「日付」を「作成日」と「更新日」に分割（合計7種のソート基準）
  - 更新日降順 / 更新日昇順 / 作成日降順 / 作成日昇順 / 名前 / 会話件数 / モデル

### ステータスバー改善
- 動作中エージェントの表示を改善: PID表示からエージェント名表示に変更
- `/c/tmp/agent_{エージェント名}_{タスク}.txt` ファイルを監視して動作中エージェントを特定
- 直近の監視間隔×2（最低5秒）以内に更新されたファイルを「動作中」と判定
- ツールチップに「▶ 資料作成部」のようにエージェント名を表示
- ファイルベースの検出がない場合はPID表示にフォールバック

---

## v0.2.0 (2026-04-02)
### エージェント管理基盤の再設計

#### 拡張機能設定画面
- VS Code標準の設定UIに7つの設定項目を追加（`contributes.configuration`）
- エージェント監視間隔（秒）: ポーリング間隔を1〜60秒で調整可能
- デフォルトのソート順 / グループ化: 起動時に設定値を自動適用
- ルールフォルダパス: session-manager.jsonの値をVS Code設定でフォールバック
- 表示する最大セッション数: 大量セッション時のパフォーマンス調整
- プレビューにAIの思考過程を表示: thinkingブロックをセッションプレビューに表示
- ゴミ箱フォルダ: 削除セッションの移動先をカスタマイズ

#### アイコン変更
- 組織図の▶（履歴表示）ボタンを時計SVGアイコンに変更
- 組織図の⚡（Claude Code）ボタンをClaudeカラー（#D97706）の稲妻SVGに変更
- 凡例もSVGアイコンに更新

#### セッション紐づけメニュー改善
- 紐づけ済みセッションに `[○○に紐づけ済み]` ラベルを表示
- 他エージェントに紐づけ済みのセッションを選択した場合、上書き確認ダイアログを表示
- タイトルを「セッションを変更」/「セッションを紐づけ」で動的切替

#### ルールフォルダ設定
- `ManagerData`に`ruleFolder`フィールドを追加（デフォルト: `.agent-rules/`）
- ルールファイル名のみの指定に対応（ルールフォルダと自動結合）
- エージェント登録フォームのプレースホルダーを動的変更

#### 作業フォルダの注意書き
- エージェント登録フォームに作業フォルダの用途説明を追加

#### ソート機能
- 会話一覧ビュータイトルバーにソートボタン追加
- ソート基準: 日付（新/古）、名前、メッセージ数、モデル

#### グループ化切り替え
- 会話一覧ビュータイトルバーにグループ切替ボタン追加
- グループモード: 日付別、タグ別、エージェント別、フラット（グループなし）

#### セッション削除
- セッションの右クリックから「セッションを削除」で `~/.claude/.trash/` に移動
- 関連データ（ブックマーク・タグ・メモ・エージェント紐づけ）を一括クリーンアップ
- 紐づき済みエージェントがある場合は確認ダイアログで通知

#### ウェルカム画面
- エージェント未登録時にウェルカムメッセージと「取締役を登録」ボタンを表示
- 取締役プリセット（Opus・固定・全体統括）で即座に開始可能

#### エージェント運用ガイド
- guide.html にセクション11「エージェント運用ガイド」を追加
- README.md に「エージェント運用ガイド」セクションを追加（取締役登録→部署追加→ルール育成→引き継ぎ→おすすめ構成例）

#### .agent-rules ディレクトリ
- ルールファイルフォルダを `agent-rules/` → `.agent-rules/` にリネーム（ドットプレフィックスで隠しフォルダ化）

#### 使い方ガイド（guide.html）v0.2.0 対応
- guide.html を v0.2.0 の全機能に対応するよう全面改訂（セッション管理・エージェント管理・組織図・ステータスバー等）
- ブラウザ表示から **VS Code 内 Webview パネル**表示に変更。画像もWebview URI で正しく表示

#### 組織図からセッションを開く
- 組織図の各カードに **⚡ ボタン**（Claude Codeで開く）を追加
- 既存の ▶ ボタン（会話履歴プレビュー）と併設。凡例にも追記

#### パスコピーコマンド追加
- **セッションパスをコピー** — 会話の右クリックからJSONLファイルパスをコピー
- **メモリパスをコピー** — メモリの右クリックからファイルパスをコピー
- 他のエージェントにファイルを読ませる際に便利

#### ステータスバー表示改善
- 点滅（ブリンク）表示を廃止し、**「🟢 2 👥 5」形式**に変更
- 🟢=動作中エージェント数、👥=登録エージェント総数
- 動作中が0の場合は「👥 5」のみ表示

#### 用語・タイトル変更
- サイドバービュー名変更: 「会話一覧」→「会話一覧（セッション）」、「ブックマーク」→「会話ブックマーク」、「タグ」→「会話タグ」
- 「親部署」→「親エージェント」に全UI・ドキュメントで統一

#### エージェント設定 Webview フォーム
- **Webview パネルで設定入力** — QuickPick/InputBox のステップ式から、全項目を1画面に表示するWebviewフォームに移行
- 部署名・役割・モデル（カード型ラジオ）・セッション運用・親エージェント・作業フォルダ・ルールファイルを一括入力
- フォルダ/ファイル選択ダイアログ連携、ルールファイルひな形の自動生成ボタン付き

#### セッション一覧のエージェント名表示
- 登録済みセッションの description に `🤖部署名` を表示
- 一覧から登録済みセッションが一目でわかるように

#### プレビューヘッダのエージェント情報
- 登録済みエージェントのセッションプレビュー上部にモデル・部署名・役割バッジを表示
- 「設定編集」「ルールファイル」リンクからワンクリックで編集画面へ遷移

#### エージェントプレビュー/設定分離
- エージェント一覧でクリック → 読み取り専用プレビューを Webview で表示
- 名前・モデル・上司・役割・子エージェント一覧・ルールファイル内容を確認可能
- 「設定」ボタンで編集フォームに切り替え、セッション名からセッション履歴へ遷移

#### 用語変更: 親部署 → 親エージェント
- エージェント設定フォーム・ツールチップ・プレビューで「親部署」を「親エージェント」に統一

#### セッション操作コマンド
- **セッションを開く** — エージェント管理の右クリックから、紐づけ済みセッションを Claude Code で開く
- **セッションを新しくする** — 引き継ぎメッセージ（遺言）を旧セッションに追記し、紐づけを解除して新セッションへ移行

#### 取締役を最上位に表示
- エージェント名が「取締役」のものをツリーの最上位に表示

#### ステータスバー改善
- `~/.claude/sessions/` のJSONから全ライブセッションを検出し動作中エージェント数を表示（例: `👥 2`）
- カーソルホバーで動作中エージェント名リストをツールチップ表示
- 登録済みはエージェント名、未登録は「使い捨て (ID先頭8桁)」で表示

#### データソース統一
- **session-manager.json に一本化** — 組織図・ステータスバー・エージェント管理すべてが同じデータソースを参照するよう統一
- MDファイルパース（`エージェント一覧.md`）を廃止し、JSONストアのみに依存

#### 右クリックメニュー整理
- `setAgentRole`（エージェント役割タグ）コマンドを削除
- **未登録セッション** → 「エージェントとして登録」を表示
- **登録済セッション** → 「エージェント設定を編集」「ルールファイルを編集」を表示
- contextValue を4種（session / sessionBookmarked / sessionRegistered / sessionRegisteredBookmarked）に拡張

#### エージェント登録フォーム改善
- **ステップバイステップ入力** — 部署名（必須）→ 役割 → モデル選択 → セッション運用（固定/使い捨て）→ 親エージェント → 作業フォルダ
- 各項目に説明文を表示
- **編集フォーム** — 既存値をデフォルト表示して編集可能
- **使い捨てエージェント** — `sessionMode: 'disposable'` で管理、UIに「使い捨て」ラベル表示

#### サイドバー「エージェント管理」
- 新規サイドバービュー `claudeAgents` を追加
- エージェント一覧をライブ状態（🟢/⚪）・モデルバッジ・ルールファイル情報付きで表示
- 右クリックメニュー: セッション紐づけ・設定編集・ルールファイル編集・削除

#### エージェント一覧インジケーター
- ルールファイルの行数・サイズを各エージェント項目に表示（トークン消費の目安）
- 未設定・ファイル未検出の状態も表示

#### ルールファイル自動生成
- 登録時にルールファイル未設定の場合、ひな形を自動生成する提案を表示
- ファイル保存ダイアログでユーザーが保存先を選択

#### 用語統一
- ステータスバー: 「稼働中」に統一
- 組織図: 「組織図を開く」に統一
- 全UI文言を日本語に統一

---

## v0.1.9 (2026-04-01)
### ライブセッション検出の改善
- **PID生存チェック** — `process.kill(pid, 0)` でプロセス存在を確認し、終了済みのゾンビJSONを自動削除
- **定期ポーリング** — 5秒間隔のフォールバックポーリングを追加（Windowsで `fs.watch` イベントが漏れる問題への対策）
- ポーリングは変化検出時のみツリー更新（無駄な再描画を回避）

## v0.1.8 (2026-04-01)
### エージェント管理
- **エージェント登録** — セッションを右クリック →「エージェントとして登録」で名前・役割・モデル・ルールファイル・親エージェント・許可ツールを設定。`~/.claude/session-manager.json` に永続保存
- **エージェント役割タグ** — `[agent]部署名` 形式のタグでセッションにエージェント役割を付与・削除
- **ルールファイルを編集** — 右クリックから登録済みエージェントのルールファイル（.md）を直接編集
- **ステータスバー** — 左下にエージェント稼働数をリアルタイム表示（例: `⬟ 2/5 アクティブ`）。クリックで組織図を開く
- **エージェント組織図 Webview** — 階層構造・モデルバッジ・セッション状態（ライブ/オフライン）を一覧表示。▶ボタンからセッションをプレビュー

### 子エージェントのツリー表示
- `subagents/` フォルダ内の JSONL を自動検出・読み込み
- `isSidechain: true` のセッションを子エージェントとして識別し、親セッション配下にツリー表示
- `meta.json` から `agentType` / `description` を読み込み、タイプ別アイコンで表示
  - 🔍探索（Explore）/ 📋計画（Plan）/ 🔧汎用（general-purpose）/ 📖ガイド（claude-code-guide）

### プレビュー強化
- **Markdownレンダリング** — コードブロック・テーブル・見出し・太字・リスト・インラインコードに対応
- **リンクのクリック対応**
  - URLリンク（`https://...` または `[text](url)` 構文）→ ブラウザで開く
  - Windowsファイルパス（`C:\path\to\file.ts:42` 形式）→ エディタで開く（行番号ジャンプ付き）
  - プレースホルダー方式で二重マッチを防止
- **エージェントバッジ** — 登録済みエージェントのセッションはプレビュー上部にモデル・名前・役割を表示

### その他
- **セッションIDコピー** — 右クリックからIDをワンクリックでクリップボードにコピー
- **VS Code フォーク対応** — Antigravity 等の VS Code フォークで「Claude Codeで開く」が正しく動作
- **README 全面改訂** — 全機能の説明・コマンド一覧・アイコン凡例を整備

## v0.1.4 (2026-03-30)
- メモリ管理のOneDrive対応（マルチバイト文字を含むパスの正しい表示）

## v0.1.3 (2026-03-28)
- 両PC変更統合・MEMORY.md表示/編集対応

## v0.1.2 (2026-03-28)
- メモリ管理の各ファイルに行数表示を追加

## v0.1.1 (2026-03-28)
- displayNameに「セッション履歴管理」サブタイトルを追加
- VS Code Marketplace に初回公開

## v0.1.0 (2026-03-28)
初回リリース

### 会話管理
- 全プロジェクトの会話を日付別（今日/昨日/今週/今月/それ以前）に自動分類
- チャット形式プレビュー（ヘッダ情報+メモ欄+会話内容）
- ブックマーク・タグ・メモ・リネーム
- タイトル・プロジェクト・ブランチ名で検索
- 会話内キーワード検索
- 右クリックからClaude Codeで会話を再開
- モデル頭文字（Ｏ/Ｓ/Ｈ）とメッセージ件数を表示
- リネーム時は元メッセージをdescriptionに表示
- 他プロジェクトのセッションを薄い色で表示
- Claude Code自動生成タイトル（ai-title）に対応

### ライブセッション検出
- Claude Codeで使用中のセッションをリアルタイム検出（緑●アイコン）

### メモリ管理
- タイプ別バッジ付きファイル一覧
- プログレスバー付きインデックス使用率表示
- プレビュー・編集・統合・抽出・削除
- 各ファイルの行数表示

### アイコン体系
- 通常: primitive-dot（小さい点）
- ブックマーク: star-full（黄）
- 利用中: circle-filled（緑）
- プレビュー中: eye
- プレビュー+利用中: target（緑）
- モデルはアイコンではなくラベル頭文字（Ｏ/Ｓ/Ｈ）で表示

### その他
- Claudeオレンジの拡張機能アイコン
- 使い方ガイド（guide.html）をブラウザで開くコマンド
- Claude風配色のガイドページ
