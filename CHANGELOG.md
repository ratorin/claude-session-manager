# 更新履歴

ユーザー向けの主な変更点のみを記載しています。実装詳細・レビュー修正・見送り事項などの
内部作業ログは Git のコミットログを参照してください。

---

## v0.5.33 (2026-07-27) — リンク切れセッションの可視化と復旧

> 注: v0.5.32 は一部が先行公開されていたため、本リリース（0.5.33）に全機能を統合しています。

エージェントに紐づいた `sessionId` の JSONL が `~/.claude/projects/` から消えている（ローテーション・
削除済み）場合、これまでは会話タイトルの代わりにセッション ID の先頭 8 文字が表示され、しかも
「Claude で開く」を押しても空のウィンドウが開くだけで復旧できませんでした。本バージョンで:

- リンク切れを検出し、一覧に **「⚠ リンク切れ」** と警告アイコンで明示（ID 直書きを廃止）。
- 該当エージェントを右クリック（またはインラインの ⚠ ボタン）から **「リンク切れセッションを解除／再紐づけ」** を実行可能に。
- 「Claude で開く」「ターミナルで開く」等の各経路で、JSONL 欠損時に空ウィンドウを開かず、再紐づけ／解除へ誘導。

**セッションの自動バックアップ＆復元（新機能）:**

Claude Code は既定で 30 日を過ぎた会話ログを自動削除し（`cleanupPeriodDays`）、この設定の存在を
知らないうちにエージェントの紐づけが切れてしまいます。設定に頼らず CSM 側で守ります。

- 紐づけ済みセッションの会話ログを `~/.claude/csm-session-backups/` へ**定期的に増分バックアップ**
  （既定 60 分間隔、変更が無いセッションはコピーしない）。
- リンク切れを検出したエージェントは、右クリックのダイアログから **「バックアップから復元」** で
  ワンクリック復旧できます（バックアップがある場合）。
- 起動時、`cleanupPeriodDays` が短い場合は**一度だけ延長を案内**（知らないユーザーの取りこぼし防止）。
- **バックアップ管理画面**（コマンド「セッションバックアップを管理」/ エージェント管理ツールバー）。
  一覧を表形式で表示（サイズ・更新日時・紐づけ先・孤立判定）、合計容量、選択/孤立一括/「N日以上前」を
  **ゴミ箱へ移動**・フォルダを開く に対応。
- **削除はすべてゴミ箱方式（復元可能）** — 削除操作は `~/.claude/csm-session-backups/.trash/` へ退避するだけで、
  実際の容量解放は管理画面の **「ゴミ箱を空にする」**（完全削除・確認あり）を押したときだけ行われます。
- **自動クリーンアップ**（**既定オフ**・安心して有効化するまで何もしません）: 有効時のみ、**孤立**
  （エージェント・お気に入り・タグ・カスタム名・メモのいずれにも紐づかない）かつ `autoCleanupDays`
  （既定180日）を過ぎたバックアップをゴミ箱へ移動します。**紐づけ済み・お気に入りは対象外**。
  保護対象が 0 件（データ読み取り異常の疑い）のときは安全側で自動処理をスキップします。
- 設定: `sessionBackup.enabled` / `intervalMinutes`（既定60）/ `maxFileSizeMB`（既定100）/
  `warnShortCleanupPeriod` / `autoCleanupOrphaned`（**既定オフ**）/ `autoCleanupDays`（既定180）。

**不具合修正:**

- エージェント設定フォームの「セッションから読み込み」で作業フォルダに変換済み表示パス（`session.project`）が
  入り、cwd 欠落時の推測パスや dev-lamp の Linux 変換パスが混入していたのを修正。生の cwd を優先するように変更。
- **「取締役」プリセットが保存できない不具合を修正** — 名前（識別名）に日本語 `取締役` が入っていて ASCII 必須の
  バリデーションで無言に弾かれていました。プリセットを `name: director` / `displayName: 取締役` に変更。
- 名前が空・不正なまま保存しても**無反応だったのを、明確な警告表示に変更**（「日本語は表示名欄へ」の案内付き）。
- **保存先（グローバル / プロジェクト）の表示を明確化** — 「プロジェクト」は今開いているワークスペースの
  `.claude/agents` であることを実パス付きで明示。フォルダ未オープン時はプロジェクトを選べないようにし、
  作業フォルダがワークスペース外の場合は注意を表示（保存が無言で失敗するのを防止）。

**Claude Code 2.1.220 追従:**

- リンク切れ判定を Claude Code 公式のライブメタ（`~/.claude/sessions/*.json`）と突き合わせ、稼働中セッションを
  誤ってリンク切れ表示しないよう強化（transcript 未フラッシュの一瞬でも誤検知しない）。
- 2.1.220 のセッションメタ新フィールド `procStart` をスキーマに取り込み。確認バージョン表記・推奨 Claude Code
  バージョンを 2.1.220 に更新。`sessions/*.json` に `agent` フィールドが付与されれば自動追従できるよう型は整備済み。

**表記の更新:**

- モデル説明の版数直書き（`Opus 4.8` / `Fable 5`）を **`（最新世代）`** に統一。`opus` 等のエイリアスが起動時に
  最新モデル（Opus 5 など）へ解決する挙動と一致し、今後の版更新で古くならないようにしました。
- 利用制限モニターの表記を Claude の現行 2 枠（**セッション(5h) / 全モデル(週)**）に更新。廃止された
  「Sonnet 5日 / Opus 5日」枠の記述を設定説明・README から削除（該当ヘッダが API から提供されなくなったため、
  従来から自動的に非表示になっていました）。

## v0.5.31 (2026-07-16) — ドキュメント整理

Marketplace 公開に向けて CHANGELOG / README / guide.html を整理。実装詳細や見送り事項を削り、
ユーザー向けの要点に絞り込みました。過去バージョンの完了済み設計・QA レポートは
`docs/archive/` に退避（コード変更なし）。

2026-07-17: 品質管理部の公開前 QA レビュー結果（HIGH 1 / MEDIUM 3 / LOW 4）に対応。guide.html の
`effort=max` 記載を「全モデル選択可」に修正、旧カード操作 Tip と `v0.4.2〜v0.4.4 新機能ハイライト`
節を撤去、モデル版数のハードコード（`Opus 4.8` / `Fable 5`）を「最新世代」表記に統一、ライブ状態
ビューの独立解説を追加。README の変更履歴に v0.5.22 を追記。`docs/v0.6.0-roadmap.md` の archive 退避に伴う
リンク切れを修正、組織図の記述を Canvas 実装に更新。

2026-07-24: 推奨 Claude Code バージョン表記を実態に合わせて更新（旧「2.1.19x 以降」は未来の期待値だったため、
現行の 2.1.17x 系（動作確認 2.1.178）に修正）。

## v0.5.30 (2026-07-16) — エージェント一覧の起動高速化

エージェント管理ツリーを 2 段レンダリング化。メモリ上のセッションタイトルだけで一覧を即描画し、
他プロジェクトのセッションタイトル解決は非同期で追いつく方式に変更。エージェント定義キャッシュを
2 秒 → 10 秒に延長。多数エージェント環境でも一覧が数十 ms で表示されるようになりました。

## v0.5.29 (2026-07-14) — 「Claude で開く」の新ウィンドウ挙動を全経路で統一

エージェント管理右クリック・エージェントプレビュー・会話一覧・ライブ状態未定義・オーケストレーション・
組織図ノード・会話ビューワーヘッダの全 7 経路で、対象フォルダがワークスペース外の場合に
自動で新しい VS Code ウィンドウを開く挙動に統一しました（設定 `agent.openInNewWindowWhenFolderMismatch`、
既定 ON）。

## v0.5.28 (2026-07-13) — v0.5.27 レビュー修正

「Claude で開く」時の警告メッセージ復活、`revealFolder` の存在確認とエラー通知、UNC パス
（`\\server\share`）の変換保持など、堅牢性の穴 4 件を修正。

## v0.5.27 (2026-07-13) — エージェント基本情報にフォルダパス + 新ウィンドウ起動

エージェントプレビューの基本情報にフォルダ行を追加（クリックで OS のファイルエクスプローラで開く）。
「Claude で開く」で対象フォルダがワークスペース外なら新しい VS Code ウィンドウを自動起動
（設定 `agent.openInNewWindowWhenFolderMismatch`、既定 ON）。

## v0.5.26 (2026-07-13) — 組織図の整理

グローバルエージェントの混入バグを修正、ルート絞り込みセレクタ追加、階層モードを縦型
インデントツリー（ファイルツリー風、折りたたみ対応）に再設計、グラフモードに親→子矢印と
線の太さ調整で視認性改善。

## v0.5.25 (2026-07-13) — 組織図グラフのズーム/パン対応

ホイールでカーソル基点ズーム、背景ドラッグでパン、ダブルクリック / ⤢ ボタンで全体フィット。
ツールバーに ± / ⤢ ボタン、右下に倍率バッジ。初期表示は全ノードが収まるようフィット、
ラベルはズーム倍率に反比例して読みやすさを自動補正。

## v0.5.24 (2026-07-13) — ライブ状態ツリー化 + cwd 推測マッチング撤去

ライブ状態ビューをフラット → エージェント別 2 階層ツリーに変更。同一 workDir を共有する
複数エージェント間で通常チャット窓が別エージェントに誤って貼り付いていた実害
（cwd 推測マッチング）を撤去し、`sessionId` 一致のみを使用。本物紐付けの無いセッションは
「未定義」グループに集約（設定 `agents.showUnregisteredLive`）。

## v0.5.23 (2026-07-13) — 組織図リデザイン

Obsidian 風力学グラフ（Canvas 自前実装、ダーク固定）に全面刷新。階層モード（カード階層、
テーマ追従）とグループモード（部署別 / モデル別 / 稼働状態別）のサブモード追加。
`/csm-ask-agent` 送信履歴の連携レイヤー、現ワークスペース外エージェントの減光機能。
Cytoscape / ELK 依存を撤去（旧「関係」「グループ」の真っ暗バグも実装ごと消滅）。

## v0.5.22 (2026-07-13) — Claude Code 追従スプリント

`sessions/*.json` の公式メタ（`kind` / `entrypoint` / `version` / `name` / `nameSource` / `agent`）を
活用しオーケストレーションビューとライブビューの表示を改善。`claude agents --json` 依存を撤去し
PID + `sessions/*.json` 監視のみで完結。`effort=max` を全モデル選択可に緩和。fast mode 非対応を明記。

## v0.5.21 (2026-07-10) — Marketplace 公開前のドキュメント整備

README / guide.html / package.json の文言統一（コード変更なし）。

## v0.5.20 (2026-07-10) — 会話ビューワーの起動高速化

末尾遅延読み込み + tail リーダー + 巨大メッセージ抑制で、数百 MB の JSONL でも
体感 1 秒以内で開けるように。設定: `preview.initialMessages`（既定 200） /
`preview.maxMessageBytes`（既定 4096）。

## v0.5.19 (2026-07-09) — 会話ビューワーヘッダに「Claude で開く」ボタン

拡張 UI で会話を再開する `▶ Claude で開く` と、CLI（新規ターミナル）で開く
`⌨ CLI で開く` の 2 ボタンに分離。

## v0.5.18 (2026-07-09) — UX 改善 Sprint C-2

Get Started ウォークスルー（5 ステップ）、エージェント数バッジ、稼働中のみ表示フィルタ、
グループ表示切替（組織図 / モデル別 / 状態別 / フラット）、アクティビティバー 5 → 4 に整理
（オーケストレーションはエージェントコンテナ内へ移設）、組織図の検索・凡例チップ、
VS Code テーマ追従の強化。

## v0.5.17 (2026-07-09) — UX 改善 Sprint C-1

エージェント検索コマンド、ステータスバー表示スタイル切替（`full` / `compact` / `max-only`）、
英日混在の日本語ラベル統一、セッション行 `description` 構成のカスタマイズ、タブバー非表示化設定。

## v0.5.16 (2026-07-09) — Sprint B: 品質改善

MEDIUM / LOW 修正 12 件 + ワークスペース内セッションが灰色表示になるバグ修正。
主に安定性・体感速度の底上げ。

## v0.5.15 (2026-07-09) — セッション詳細ビューに「Claude で開く」ボタン

会話ビューワーのヘッダに Claude Code 拡張を起動するボタンを追加。

## v0.5.14 (2026-07-09) — Fable 5 モデル解禁

`fable` / `fable-1m` を第一級モデルとして復活（表示文字 Ｆ、色 金 #ffd54f）。
`effort: max` も選択可能。関連バグ修正: `claude-fable-5[1m]` の判定順序、
`fable → opus` のサイレント書き換え防止。

---

## v0.5.0 〜 v0.5.13（2026-04 〜 2026-06）

Claude Code 統合・エージェント管理基盤・利用制限モニター・フックライフサイクル・
オーケストレーションビューなど v0.5 系の基盤機能を段階的に構築した期間。
詳細な変更内容はコミットログ（`git log --oneline`）を参照してください。

主なマイルストーン:

- **v0.5.13** — フォームに `maxTurns` を追加
- **v0.5.9 〜 v0.5.12** — 利用制限ステータスバーの残量表示・追加分（overage）表示
- **v0.5.6 〜 v0.5.8** — Opus 1M 追加、hook 除去 QA、モデル選択整理
- **v0.5.2 〜 v0.5.5** — Hook ライフサイクル堅牢化、Agent QA、クロス OS cwd バグ修正
- **v0.5.1** — オーケストレーション可視化タブ、Activity Bar 4 アイコン化、モデル追従、
  タブバー統一など小改善を頻繁にリリース
- **v0.5.0** — Claude Code 統合・PID/JSONL ベースのライブ状態検知・エージェントタブ
  3 分割・[Open]/[経過時間] プレフィックス追加
