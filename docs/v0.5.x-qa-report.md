# v0.5.2〜v0.5.7 QA レポート（2026-06-01）

最近の更新（hook ライフサイクル堅牢化・フォーム新項目・クロスOS cwd 修正・モデル更新・整合性修正）を QA。
自動ヘルス + 専門コードレビュー + 横断点検を実施。

## サマリ
- **ビルド健全性**: `tsc` クリーン / **全 31 テスト合格** / VSIX パッケージ成功。
- **CRITICAL: 0 件**。
- HIGH 2 件・MEDIUM 1 件・LOW 1 件を**修正**。残りは「非バグ」「設計上許容」「既存・別件」と判定。

## 修正した指摘

| ID | 重大度 | 内容 | 修正 |
|----|--------|------|------|
| HIGH-1 | HIGH | `removeSessionAgentInjectHook` が `command.includes(marker)` 判定で **exec-form hook を除去できない**（dead code だが潜在バグ） | `hookMatchesMarker` 使用に変更（exec-form 対応） |
| HIGH-2 | HIGH | `ensureSessionAgentInjectHook` が exec-form マイグレーション後に無条件 `return false` で**変更が永続化されない** | `migratedToExecForm` を追跡し `return` に反映 |
| MEDIUM-4 | MEDIUM | settings.json の `.bak.*` が**無制限に蓄積** | `pruneOldSettingsBackups()`（最新5件保持）を新設し、書き込み箇所から呼び出し |
| LOW-1 | LOW | `isForeignOsPath` の Windows 分岐の意図がコメント未記載 | 意図（POSIX 絶対パスを外来扱い）を明記 |

## 修正不要と判定した指摘

| ID | 判定 | 根拠 |
|----|------|------|
| MEDIUM-1（description テキストエリアが dead UI） | **レビューの誤り（一部）** | `getFormData` は description を収集済（textarea 値は role に流入）。`description` プロパティが未使用なだけで dead ではない。混乱しうる UX だが既存仕様・挙動変更リスクのため非修正 |
| MEDIUM-2（`allowedTools=[]` が永続化されない） | **非バグ** | `[]`→tools フィールド無し＝全継承（無制限）が正しいセマンティクス。テスト E3 で「解除が反映される」ことを検証済 |
| MEDIUM-3（`modifySettingsJson` の `.catch` がエラーを握り潰す） | **設計上許容** | 内部でバックアップ+JSON検証+復元の安全網があり、検証失敗時はファイル復元済み。起動時 fire-and-forget の hook 登録でクラッシュさせない設計。リファクタの回帰リスク > 便益 |
| LOW-2（`translateText` に fetch タイムアウト無し） | **既存・別件** | オフライン時フォームが待つ可能性。日本語名自動生成の補助機能。優先度低 |
| LOW-3（`createRenewSession` の UUID 検証無し） | **既存・別件** | ローカル CLI 出力のためリスク低。一貫性のみの軽微指摘 |
| LOW-4（フォーム ASCII vs backend Unicode 名検証差） | **意図的** | CLI のエージェント名は ASCII が正。フォームが厳しめなのは妥当 |

## 確認済み（OK）
- `modifySettingsJson` の排他書き込みキュー（直列化・TOCTOU 防止）
- `healForeignOsHookPaths` の heal→prune 順序、誤 prune しないガード
- `buildHookDef`/`hookMatchesMarker` の shell/exec 両対応
- `quoteYamlValue`/`parseFrontmatterExtended` のラウンドトリップ（`\\ " \n \r \t`）
- `isValidAgentName` のパストラバーサル防止 + 二重チェック
- `resolveSpawnCwd` のクロスOS変換 + フォールバック
- `spawn` の `shell:false`、`uninstall.ts` の vscode 非依存
- `filterCsmHooks` の冪等性、`normalizeModel` の fable→opus フォールバック
- opus-1m のモデル表示/不一致検出/色/ラベル整合（v0.5.7 で修正済）

## テスト
- `test/unit/agent-hooks-qa.test.js`: 31 ケース（binding / agentFileManager / hookスクリプト / hookクリーンアップ / workDir変換 / モデル / バックアップ世代管理）。全合格。
